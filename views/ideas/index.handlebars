{{#each ideas}}
    <div class="card card-body mb-3">
        <h4>{{title}}</h4>
        <p>{{details}}</p>
        <p>{{date}}</p>
        <a class="btn btn-dark btn-block" href="/ideas/edit/{{id}}">Edit</a>
    </div>
{{else}}
    <p>No notes listed</p>
{{/each}}